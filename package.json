{
  "name": "ausus-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AUSUS monorepo — npm workspace for @ausus/renderer-react + playground.",
  "license": "MIT",
  "workspaces": [
    "renderer/react",
    "apps/playground/web"
  ],
  "scripts": {
    "build":     "npm run --workspace @ausus/renderer-react build",
    "trace":     "npm run --workspace ausus-playground-web trace",
    "bundle":    "npm run --workspace ausus-playground-web bundle",
    "clean":     "rm -rf node_modules renderer/react/node_modules renderer/react/dist apps/playground/web/node_modules apps/playground/web/dist",
    "smoke":     "npm run build && npm run trace"
  }
}
