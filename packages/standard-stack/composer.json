{
    "name": "ausus/standard-stack",
    "type": "metapackage",
    "version": "0.1.0",
    "description": "AUSUS — meta-package installing the V0 Standard Stack (kernel + persistence-sql + runtime-default). Skeleton packages (tenancy-row, audit-database, auth-bridge, presentation-default) will be added as their RFC-12 §16 implementations land.",
    "license": "MIT",
    "keywords": ["ausus", "framework", "stack"],
    "homepage": "https://github.com/ausus-framework/ausus",
    "require": {
        "php": ">=8.3",
        "ausus/kernel":           "0.1.*",
        "ausus/runtime-default":  "0.1.*",
        "ausus/persistence-sql":  "0.1.*"
    },
    "extra": {
        "ausus": {
            "role": "meta",
            "v0-scope": ["ausus/kernel", "ausus/runtime-default", "ausus/persistence-sql"]
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
